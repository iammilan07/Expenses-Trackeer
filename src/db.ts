import PouchDB from 'pouchdb';

const DB = () => {
    // const constructor = (name: any) => {
    //     var db = new PouchDB(name);
    // }
    // async getAllNotes(){
    //     let allNotes = await db.allDocs({ include_docs: true });
    //     let notes = {};

    //     allNotes.rows.forEach((n: any) => notes[n.id] = n.doc);
    //     return notes;
    // }

}
export default DB